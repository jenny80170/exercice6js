function valider(){
  var saisie = document.getElementById('number').value;
  window.alert(saisie * 10);
  }

function emptyField(){
  document.getElementById('number').value='';
}

var number = document.querySelector ('input').value;
